<Window x:Class="Releaser.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Releaser.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Background="#525252">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Vertical">
            <Button Background="#252526" Foreground="#F1F1F1" x:Name="SwitchOnOff" Content="{Binding SwitchOnOffContent}"></Button>
            <Button x:Name="Refresh">Manual refresh</Button>
            <TextBlock Foreground="#F1F1F1" Text="{Binding Path=ContactsCount, Mode=OneWay}"/>
            <TextBlock Foreground="#F1F1F1" Text="{Binding Path=LastAttempt, Mode=OneWay}"/>
            <TextBlock Foreground="#F1F1F1" Text="{Binding Path=LastSuccessfulAttempt, Mode=OneWay}"/>
            <TextBlock Foreground="#F1F1F1" Text="{Binding Path=LastAttemptStatus, Mode=OneWay}"/>
        </StackPanel>
        <ItemsControl Grid.Column="1" x:Name="Contacts">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="#252526" BorderBrush="#3F3F46" BorderThickness="1" Margin="5" Padding="5">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Foreground="#F1F1F1" Text="{Binding Username}" />
                            <TextBlock Foreground="#F1F1F1" Text="{Binding AmountRub}" />                            
                            <TextBlock Foreground="#F1F1F1" Text="{Binding MarketAsPaid}" />                            
                            <Label Foreground="#F1F1F1" Content="Releeznoot'"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>